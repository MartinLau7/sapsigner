#include <stdbool.h>

#include "CFStringGetCString.h"

Boolean CFStringGetCString(CFStringRef theString, char *buffer, CFIndex bufferSize, CFStringEncoding encoding) {
    return true;

    (void) theString;
    (void) buffer;
    (void) bufferSize;
    (void) encoding;
}
